<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All friend requests
    </h1>

    <a href="/friend_requests/new" class="btn btn-block btn-outline-secondary">
      Add a new friend request
    </a>
  </div>
</div>

<div class="row">
  <div class="col-md-12 mb-3">
    <%= render partial: "friend_request_templates/friend_requests_search_form" %>
  </div>
</div>



<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Sender
        </th>

        <th>
          Recipient
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @friend_requests.each do |friend_request| %>
      <tr>
        <td>
          <%= friend_request.id %>
        </td>

        <td>
          <% if friend_request.sender.present? %>
            <a href="/users/<%= friend_request.sender.id %>">
              <%= friend_request.sender.username %>
            </a>
          <% end %>
        </td>

        <td>
          <% if friend_request.recipient.present? %>
            <a href="/users/<%= friend_request.recipient.id %>">
              <%= friend_request.recipient.username %>
            </a>
          <% end %>
        </td>

        <td>
          <%= time_ago_in_words(friend_request.created_at) %> ago
        </td>

        <td>
          <%= time_ago_in_words(friend_request.updated_at) %> ago
        </td>

        <td>
          <a href="/friend_requests/<%= friend_request.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @friend_requests, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
